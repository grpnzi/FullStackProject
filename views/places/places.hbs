<h2>List of Places</h2>

{{#each places}}
  <a href="/places/{{_id}}">{{name}}</a>
  <img src="{{img}}" alt="{{name}}">
  
  {{#each author}}
    <p>Created by {{this.username}}</p>
  {{/each}}
  
  <p>{{source}}</p>
  <p>{{title}}</p>
{{/each}}